name: "Gamebuddy"
description: "App to play games with friends"
publish_to: "none"
version: 0.1.4+5

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  supabase_flutter: ^2.8.3
  provider:
  flutter_local_notifications: ^19.1.0
  permission_handler: ^12.0.0+1
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.5
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/images/
